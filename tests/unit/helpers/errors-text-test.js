import { errorsText } from 'motoshare-client/helpers/errors-text';
import { module, test } from 'qunit';

module('Unit | Helper | errors text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = errorsText([42]);
  assert.ok(result);
});
